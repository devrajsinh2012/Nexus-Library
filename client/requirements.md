## Packages
framer-motion | Page transitions and elegant scroll animations
date-fns | Date formatting for due dates and return times

## Notes
- Auth state is managed via `/api/auth/user`. Uses session-based local authentication.
- Login redirect is `/api/login`, logout is `/api/logout`.
- Earthy, premium library aesthetic with serif typography.
- Uses Unsplash for placeholder book covers.
